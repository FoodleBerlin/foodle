import * as Setset from 'setset';
export var Runtime;
(function (Runtime) {
    Runtime.settings = Setset.create({
        fields: {
            prismaClientContextField: {
                initial: () => 'prisma',
            },
        },
    });
    function changeSettings(input) {
        Runtime.settings.change(input);
    }
    Runtime.changeSettings = changeSettings;
})(Runtime || (Runtime = {}));
//# sourceMappingURL=settingsSingleton.js.map