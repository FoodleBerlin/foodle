import { DMMF } from '@prisma/generator-helper';
import { LiteralUnion } from 'type-fest';
import { StandardGraphQLScalarType } from '../../helpers/graphql';
import { Gentime } from '../gentime/settingsSingleton';
import { ModuleSpec } from '../types';
export declare const createModuleSpec: (dmmf: DMMF.Document, settings: Gentime.Settings) => ModuleSpec;
export declare function renderTypeScriptDeclarationForDocumentModels(dmmf: DMMF.Document, settings: Gentime.Settings): string;
/**
 * Map the fields type to a GraphQL type.
 *
 * @remarks The `settings` param type uses settings data instead of Setset instance because this helper
 *          is used at runtime too where we don't have a Setset instance for gentime.
 */
export declare function fieldTypeToGraphQLType(field: DMMF.Field, settings: Gentime.SettingsData): LiteralUnion<StandardGraphQLScalarType, string>;
