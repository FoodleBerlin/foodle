import * as Setset from 'setset';
export declare namespace Runtime {
    type SettingsInput = {
        /**
         * The name of the GraphQL context field to get an instance of Prisma Client from.
         *
         * This instance of Prisma Client is accessed in the default resolvers for relational fields.
         *
         * @default prisma
         */
        prismaClientContextField?: string;
    };
    type SettingsData = Setset.InferDataFromInput<SettingsInput>;
    type Settings = Setset.Manager<SettingsInput, SettingsData>;
    const settings: Setset.Manager<SettingsInput, Setset.InferDataFromInput<SettingsInput>>;
    function changeSettings(input: Setset.UserInput<SettingsInput>): void;
}
