import { Num } from './level.js';
import * as RootLogger from './root-logger.js';
declare type Context = Record<string, unknown>;
export declare type LogRecord = {
    level: Num;
    event: string;
    path?: string[];
    context?: Context;
    time?: number;
    pid?: number;
    hostname?: string;
};
declare type Log = (event: string, context?: Context) => void;
export declare type Logger = {
    fatal: Log;
    error: Log;
    warn: Log;
    info: Log;
    debug: Log;
    trace: Log;
    addToContext: (context: Context) => Logger;
    child: (name: string) => Logger;
};
/**
 * Create a logger.
 */
export declare const create: (rootState: RootLogger.State, path: null | string[], parentContext?: Context) => {
    logger: Logger;
    link: Link;
};
declare type Link = {
    onNewParentContext: (newContext: Context) => void;
};
declare type State = {
    pinnedAndParentContext?: Context;
    children: Link[];
};
export {};
//# sourceMappingURL=logger.d.ts.map